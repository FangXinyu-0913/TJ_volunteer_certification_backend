server:
  port: 8082

# datasource is in nacos

#mybatis-plus:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
spring:
  output:
    ansi:
      enabled: detect
  datasource:
    url: jdbc:mysql://1.117.102.181:3306/Certification
    username: root
    password: fx20020913
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
  jackson:
    serialization:
      indent-output: false
#  main:
#    web-application-type: reactive